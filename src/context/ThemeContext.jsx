import { ThemeProvider, createTheme } from '@mui/material/styles';
import CssBaseline from '@mui/material/CssBaseline';
import { useContext } from 'react';


const ThemeContext = useContext('')
const darkTheme = createThem= ({
  palette: {
    mode: 'dark',
  },
});